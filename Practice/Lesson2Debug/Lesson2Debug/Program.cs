using System;

namespace Lesson2Debug
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Your task is to:
                1. Identify and fix the 3 syntax errors in this program.
            a,b: missing semicolon
            c: extra } at end of document
                2. Run the program after fixing to confirm it works as intended.
                3. Submit your corrected code and briefly explain each fix.
            */
            string name = "Jordan";
            int age = 25;

            Console.WriteLine("Name: " + name);
            Console.WriteLine("Age: " + age);
        }
    }
}