@page "/device-info"
@using MauiDeviceAccessLesson5.Services
@inject DeviceService Service

<h3>Device Info Demo</h3>
 
<p><strong>Model:</strong> @deviceModel</p>
<p><strong>Manufacturer:</strong> @manufacturer</p>
<p><strong>OS Version:</strong> @osVersion</p>
 
<h3>Battery</h3>
 
@if (batteryLevel is null)
{
    <p><strong>Level:</strong> Not available</p>
}
else
{
    <p><strong>Level:</strong> @batteryLevel%</p>
}
 
<p><strong>Power Source:</strong> @powerSource</p>
 
<button class="btn btn-primary" @onclick="LoadData">Refresh</button>
@code {
    private string deviceModel = "";
    private string manufacturer = "";
    private string osVersion = "";
    private int? batteryLevel = null;
    private string powerSource = "";


    private void LoadData()
    {
        deviceModel = Service.GetModel();
        manufacturer = Service.GetManufacturer();
        osVersion = Service.GetOsVersion();
        batteryLevel = Service.GetBatteryLevel();
        powerSource = Service.GetPowerSource();
    }
    protected override void OnInitialized()
    {
        LoadData();
    }
}
